import XCTest

final class CryptoPortfolioTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder test — replace with real tests as features are added.
        XCTAssertTrue(true)
    }
}
